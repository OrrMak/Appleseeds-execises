
function toggleColumn(numOfColumn){
    // if(numOfColumn.isInteger()==='false'){
    //     alert("num of column must be int")
    // }
const rows=document.querySelectorAll('#dataTable tr');

const cell=[];
rows.forEach((currenRow)=>{
    const cell=currenRow.cell[index];
});    

}

console.log(rows);